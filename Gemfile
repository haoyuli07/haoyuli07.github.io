# Only needed to preview the site locally. GitHub Pages builds this site
# natively — it uses no plugins — so nothing here runs in CI.
#
#   bundle install
#   bundle exec jekyll serve   # then open http://localhost:4000

source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins
gem "webrick", "~> 1.8"
